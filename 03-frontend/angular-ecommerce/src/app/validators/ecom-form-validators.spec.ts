import { EcomFormValidators } from './ecom-form-validators';

describe('EcomFormValidators', () => {
  it('should create an instance', () => {
    expect(new EcomFormValidators()).toBeTruthy();
  });
});
